{
  "name": "afterlight",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "A local-first personal ophthalmology record and visual diary.",
  "scripts": {
    "predev": "node scripts/sync-explorer.mjs",
    "dev": "vite",
    "prebuild": "node scripts/sync-explorer.mjs",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "guard": "node scripts/check-invariants.mjs",
    "verify": "npm run guard && npm run build"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  }
}
